# if on windows
Open this in UCRT64 Shell
./build_obby.sh for the latest version

# if on linux, im trying some cmake stuff
- if you edited the CMakeLists.txt file in balls,
	- you need to rm -r build to get rid of previous build structure
	- mkdir build && cd build to create and enter new build environment
	- cmake --build . to build and compile the project

